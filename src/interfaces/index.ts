import { IClassLimits, IClassType, ILimits } from "./class-limit.interface";
import {
    IHttpStatusMappings,
    IHttpStatusCodes,
    IHttpStatusNames,
    IHttpStatusDescriptions,
    ICodeList,
} from "./http-status-codes.interface";

export {
    IClassLimits,
    ILimits,
    IHttpStatusMappings,
    IHttpStatusCodes,
    IHttpStatusNames,
    IHttpStatusDescriptions,
    ICodeList,
    IClassType,
};
